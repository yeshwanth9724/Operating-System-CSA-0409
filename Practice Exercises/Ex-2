#include <stdio.h>

int main()
{
    int p[5], n, i;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++)
        scanf("%d", &p[i]);

    for(i = 0; i < n; i++)
        printf("Executing Process %d\n", p[i]);

    return 0;
}
